# ============================================
# CONFIGURACIÓN WSL2 PROFILE
# ============================================

# Nombre de la aplicación
spring.application.name=citytransit-backend

# Puerto del servidor
server.port=8080

# ============================================
# BASE DE DATOS POSTGRESQL (Host Windows)
# ============================================
spring.datasource.url=jdbc:postgresql://172.23.80.1:5433/paytransit
spring.datasource.username=postgres
spring.datasource.password=redfire007
spring.datasource.driver-class-name=org.postgresql.Driver

# JPA / Hibernate
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=false
spring.jpa.properties.hibernate.format_sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.PostgreSQLDialect

# ============================================
# MONGODB (Host Windows)
# ============================================
spring.data.mongodb.uri=mongodb://admin:redfire007@172.23.80.1:27017/paytransit?authSource=admin
spring.data.mongodb.database=paytransit

# ============================================
# REDIS (Host Windows)
# ============================================
spring.data.redis.host=172.23.80.1
spring.data.redis.port=6379
spring.data.redis.password=redfire007
spring.cache.type=redis
spring.cache.redis.time-to-live=3600000

# ============================================
# CLICKHOUSE (Host Windows)
# ============================================
clickhouse.url=jdbc:clickhouse://172.23.80.1:8123/paytransit
clickhouse.username=default
clickhouse.password=redfire007

# ============================================
# GRAPHQL
# ============================================
spring.graphql.graphiql.enabled=true
spring.graphql.graphiql.path=/graphiql
spring.graphql.path=/graphql
spring.graphql.websocket.path=/graphql-ws

# ============================================
# JWT
# ============================================
jwt.secret=Y2l0eXRyYW5zaXRzZWNyZXRrZXljaGFuZ2V0aGlzaW5wcm9kdWN0aW9ubWluaW11bTI1NmJpdHNrZXk=
jwt.expiration=86400000
jwt.refresh-expiration=604800000

# ============================================
# CORS
# ============================================
cors.allowed-origins=http://localhost:3000,http://localhost:4200,http://172.23.80.1:3000
cors.allowed-methods=GET,POST,PUT,DELETE,PATCH,OPTIONS
cors.allowed-headers=*
cors.allow-credentials=true

# ============================================
# ACTUATOR (Monitoring)
# ============================================
management.endpoints.web.exposure.include=health,info,metrics,prometheus
management.endpoint.health.show-details=always
management.metrics.export.prometheus.enabled=true

# ============================================
# LOGGING
# ============================================
logging.level.root=INFO
logging.level.com.citytransit=DEBUG
logging.level.org.springframework.web=WARN
logging.level.org.springframework.security=WARN
logging.level.org.hibernate.SQL=WARN
logging.level.com.citytransit.dl=DEBUG

# ============================================
# CONFIGURACIONES PERSONALIZADAS
# ============================================
app.name=CityTransit
app.version=1.0.0
app.description=Sistema de transporte público inteligente

# Deep Learning HABILITADO en WSL2 (sin problemas de rutas)
app.dl.enabled=true
